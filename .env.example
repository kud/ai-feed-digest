# Language Configuration
# Supported: 'fr' (French), 'en' (English)
NEXT_PUBLIC_LANGUAGE=fr

# Timezone for date formatting
NEXT_PUBLIC_TIMEZONE=Europe/Paris

# OpenCode SDK Configuration
# Use external server instead of embedded mode (more reliable)
# Make sure to start server with: opencode serve --port 4096
# Or use: npm run generate:with-ai (starts server automatically)
OPENCODE_BASE_URL=http://127.0.0.1:4096

# Concurrency & Performance Tuning
# Number of parallel RSS feed fetches
FEED_CONCURRENCY=6
# Number of parallel article hydration fetches (raw HTML -> text)
HYDRATE_CONCURRENCY=4
# Number of parallel AI summary requests
SUMMARY_CONCURRENCY=4
# Maximum retry attempts for a failed summary request (1-5)
SUMMARY_RETRIES=3
# Reading speed words per minute for reading time estimates
READING_WPM=210
